apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "service:resource"
  rules:
    - actions: ["read"]
      effect: EFFECT_ALLOW
      roles:
        - role1
    - actions: ["edit"]
      effect: EFFECT_ALLOW
      roles:
        - role3
